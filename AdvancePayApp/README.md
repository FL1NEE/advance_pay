# AdvancePay Android App

Мобильное приложение для автоматической пересылки банковских уведомлений на сервер.

## Функции

- 📱 Отслеживание банковских уведомлений
- 🔄 Автоматическая отправка на backend
- 🔋 Мониторинг батареи и интернета
- 📊 История отправленных уведомлений
- 🔐 Безопасная авторизация

## Системные требования

- Android 7.0 (API 24) или выше
- Интернет подключение
- Батарея > 30% для работы

## Сборка APK

### Способ 1: Android Studio (рекомендуется)

1. **Установите Android Studio**
   - Скачайте с https://developer.android.com/studio
   - Установите Android SDK

2. **Откройте проект**
   ```
   File -> Open -> Выберите папку AdvancePayApp
   ```

3. **Настройте URL сервера**
   - Откройте `app/src/main/java/com/advancepay/data/api/ApiService.kt`
   - Измените `BASE_URL` на IP вашего сервера:
   ```kotlin
   private const val BASE_URL = "http://192.168.1.XXX:8000/"
   ```

4. **Соберите APK**
   - Нажмите `Build -> Build Bundle(s) / APK(s) -> Build APK(s)`
   - Или используйте терминал:
   ```bash
   ./gradlew assembleDebug
   ```

5. **Найдите APK**
   - Файл будет в `app/build/outputs/apk/debug/app-debug.apk`

### Способ 2: Командная строка

```bash
cd AdvancePayApp

# Windows
gradlew.bat assembleDebug

# Linux/Mac
./gradlew assembleDebug
```

APK будет в `app/build/outputs/apk/debug/app-debug.apk`

## Установка на телефон

### Через USB

1. Включите "Отладку по USB" на телефоне:
   - Настройки -> О телефоне -> 7 раз нажать "Номер сборки"
   - Настройки -> Для разработчиков -> Отладка по USB

2. Подключите телефон к компьютеру

3. Установите APK:
   ```bash
   adb install app/build/outputs/apk/debug/app-debug.apk
   ```

### Через файл

1. Скопируйте APK на телефон
2. Откройте файл на телефоне
3. Разрешите установку из неизвестных источников

## Настройка приложения

### 1. Первый запуск

- Войдите с учетными данными от backend
- Приложение сохранит токен автоматически

### 2. Разрешите доступ к уведомлениям

**ВАЖНО:** Без этого приложение не будет работать!

1. Нажмите кнопку "Разрешить доступ к уведомлениям"
2. Найдите "AdvancePay" в списке
3. Включите переключатель
4. Подтвердите разрешение

### 3. Отключите оптимизацию батареи

Чтобы приложение работало постоянно:

1. Настройки -> Батарея
2. Оптимизация батареи
3. Найдите AdvancePay
4. Выберите "Не оптимизировать"

### 4. Разрешите автозапуск

На разных телефонах по-разному:

**Xiaomi (MIUI):**
- Настройки -> Приложения -> AdvancePay -> Автозапуск

**Huawei:**
- Настройки -> Приложения -> AdvancePay -> Диспетчер приложений

**Samsung:**
- Настройки -> Приложения -> AdvancePay -> Батарея -> Разрешить фоновую активность

## Как это работает

1. **NotificationListenerService** - слушает все уведомления
2. **Фильтрация** - отбирает только банковские (Сбербанк, Тинькофф и т.д.)
3. **Парсинг** - извлекает сумму, карту, тип операции
4. **Отправка** - отправляет на backend по API
5. **Мониторинг** - проверяет батарею и интернет

## Поддерживаемые банки

- Сбербанк
- Тинькофф
- ВТБ
- Альфа-Банк
- Райффайзен
- МТС Банк
- И другие...

## Статусы работы

**✅ Работает** - батарея > 30%, есть интернет, все работает

**❌ Не работает:**
- "Низкий заряд батареи: XX%" - зарядите телефон
- "Нет подключения к интернету" - проверьте WiFi/мобильные данные
- "Доступ к уведомлениям не предоставлен" - дайте разрешение

## Устранение проблем

### Приложение не отправляет уведомления

1. Проверьте доступ к уведомлениям
2. Убедитесь что батарея > 30%
3. Проверьте интернет
4. Перезапустите приложение

### Ошибка подключения к серверу

1. Убедитесь что backend запущен
2. Проверьте IP адрес в `ApiService.kt`
3. Для эмулятора используйте `10.0.2.2`
4. Для реального устройства - IP компьютера в локальной сети

### Приложение закрывается

1. Отключите оптимизацию батареи
2. Разрешите автозапуск
3. Разрешите работу в фоне

## Backend Setup

Убедитесь что backend запущен:

```bash
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

API будет доступно по адресу:
- Локально: `http://localhost:8000`
- В сети: `http://YOUR_IP:8000`

## Логи и отладка

Для просмотра логов подключите телефон и используйте:

```bash
adb logcat | grep -i advancepay
```

Или в Android Studio: Logcat -> фильтр "advancepay"

## API Endpoints

Приложение использует следующие endpoints:

- `POST /api/v1/auth/login` - авторизация
- `GET /api/v1/auth/me` - получение инфо о пользователе
- `POST /api/v1/bank-notifications` - отправка уведомления
- `GET /api/v1/bank-notifications` - история
- `POST /api/v1/bank-notifications/device-status` - статус устройства

## Безопасность

- Токен хранится в зашифрованном DataStore
- Используется HTTPS (в production)
- Логи не содержат конфиденциальных данных

## Лицензия

Proprietary - только для внутреннего использования
