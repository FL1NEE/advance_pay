# 📱 Ручная установка APK (для Pixel 8 Pro Android 16 и MEmu)

## 🎯 Проблема решена!

Созданы изменения для максимальной совместимости:
- ✅ targetSdk понижен до 33 (работает на Android 16)
- ✅ Поддержка всех архитектур (ARM и x86 для эмуляторов)
- ✅ Опциональные разрешения для новых Android
- ✅ Debug версия с другим package name

---

## 🚀 БЫСТРАЯ СБОРКА

Запустите:
```
build_compatible.bat
```

Получите: `AdvancePay-compatible.apk`

---

## 📱 Установка на Pixel 8 Pro (Android 16)

### Способ 1: Через ADB (рекомендуется)

```bash
# Проверьте подключение
adb devices

# Должно показать ваш Pixel 8 Pro

# Удалите старые версии
adb uninstall com.advancepay
adb uninstall com.advancepay.debug

# Установите новую версию
adb install -r -d -t AdvancePay-compatible.apk
```

**Флаги:**
- `-r` = переустановка (если есть)
- `-d` = разрешить downgrade
- `-t` = разрешить тестовый APK

### Способ 2: Через файл

1. **Скопируйте APK на телефон:**
   - Подключите через USB
   - Скопируйте `AdvancePay-compatible.apk` в Download

2. **На телефоне:**
   - Откройте Files / Файлы
   - Найдите APK в Download
   - Нажмите на файл

3. **Разрешите установку:**
   - Появится "Install blocked"
   - Нажмите **Settings**
   - Включите "Allow from this source"
   - Вернитесь назад
   - Нажмите **Install**

4. **При ошибке "App not installed":**
   ```
   Настройки → Apps → Special app access → Install unknown apps
   → Files → Allow from this source
   ```

### Способ 3: Через Google Drive/облако

1. Загрузите APK на Google Drive
2. Откройте Drive на телефоне
3. Скачайте APK
4. Установите (разрешите установку из Drive)

---

## 💻 Установка на эмулятор MEmu

MEmu использует x86/x86_64 архитектуру. Новый APK поддерживает её!

### Способ 1: Перетаскивание

1. Запустите MEmu
2. Дождитесь загрузки Android
3. **Перетащите** `AdvancePay-compatible.apk` в окно эмулятора
4. Установка начнётся автоматически

### Способ 2: Через ADB

```bash
# MEmu должен быть запущен

# Найдите порт эмулятора
adb devices

# Установите
adb install -r AdvancePay-compatible.apk
```

### Способ 3: Через общую папку

1. В настройках MEmu настройте общую папку
2. Скопируйте APK в общую папку
3. В MEmu откройте File Manager
4. Найдите APK в общей папке
5. Установите

---

## 🔧 Если установка НЕ РАБОТАЕТ

### Ошибка: "App not installed"

**Причина 1: Несовместимая подпись**

```bash
# Полностью удалите старые версии
adb uninstall com.advancepay
adb uninstall com.advancepay.debug

# Очистите кэш
adb shell pm clear com.advancepay
adb shell pm clear com.advancepay.debug

# Установите заново
adb install AdvancePay-compatible.apk
```

**Причина 2: Недостаточно места**

```bash
# Проверьте место
adb shell df /data

# Если мало - удалите ненужные приложения
```

**Причина 3: Повреждённый APK**

```bash
# Пересоберите APK
cd AdvancePayApp
build_compatible.bat
```

### Ошибка: "INSTALL_FAILED_UPDATE_INCOMPATIBLE"

```bash
# Удалите ВСЕ версии приложения
adb uninstall com.advancepay
adb uninstall com.advancepay.debug

# Перезагрузите устройство
adb reboot

# Дождитесь загрузки
# Установите заново
adb install AdvancePay-compatible.apk
```

### Ошибка: "INSTALL_PARSE_FAILED_MANIFEST_MALFORMED"

APK повреждён или несовместим.

**Решение:**
1. Пересоберите APK через Android Studio
2. Используйте Build → Build APK (не Build Bundle)

### Ошибка: "Failure [INSTALL_FAILED_NO_MATCHING_ABIS]"

Архитектура не поддерживается (не должно быть - APK поддерживает все).

**Решение:**
```bash
# Проверьте архитектуру устройства
adb shell getprop ro.product.cpu.abi

# Должно быть: arm64-v8a, x86, или x86_64
```

Если показывает что-то другое - напишите мне.

---

## 🔍 Диагностика для Pixel 8 Pro

Если установка не работает, соберите информацию:

```bash
# 1. Версия Android
adb shell getprop ro.build.version.release
# Должно быть: 16 (или VanillaIceCream)

# 2. API Level
adb shell getprop ro.build.version.sdk
# Должно быть: 35 или 36

# 3. Архитектура
adb shell getprop ro.product.cpu.abi
# Должно быть: arm64-v8a

# 4. Модель
adb shell getprop ro.product.model
# Должно быть: Pixel 8 Pro

# 5. Лог установки
adb install AdvancePay-compatible.apk > install_log.txt 2>&1
# Откройте install_log.txt и покажите мне
```

---

## 🔍 Диагностика для MEmu

```bash
# 1. Проверьте запущен ли эмулятор
adb devices

# 2. Версия Android в MEmu
adb shell getprop ro.build.version.release

# 3. Архитектура
adb shell getprop ro.product.cpu.abi
# Должно быть: x86 или x86_64

# 4. Установка с логами
adb install -r -d -t AdvancePay-compatible.apk
```

---

## ✅ После успешной установки

### 1. Запустите приложение

Найдите иконку **AdvancePay** на устройстве

### 2. Настройте backend URL (для эмулятора MEmu)

По умолчанию в APK стоит `http://10.0.2.2:8000/`

**Для MEmu нужен IP компьютера!**

**ВРЕМЕННОЕ РЕШЕНИЕ:** Измените IP в коде и пересоберите:

1. Откройте: `app/src/main/java/com/advancepay/data/api/ApiService.kt`
2. Строка 41: замените на IP вашего компьютера
3. Запустите `build_compatible.bat`

Или используйте реверс-прокси:
```bash
adb reverse tcp:8000 tcp:8000
```

### 3. Войдите в приложение

Используйте логин/пароль от backend

### 4. Дайте разрешения

**КРИТИЧЕСКИ ВАЖНО:**

1. **Доступ к уведомлениям:**
   ```
   Настройки → Apps → Special access → Notification access
   → AdvancePay → Allow
   ```

2. **Отключите оптимизацию батареи:**
   ```
   Настройки → Battery → Battery optimization
   → All apps → AdvancePay → Don't optimize
   ```

3. **Для Pixel 8 Pro - автозапуск:**
   ```
   Настройки → Apps → AdvancePay → Advanced
   → Battery → Unrestricted
   ```

---

## 📊 Технические детали APK

**Package Name:** `com.advancepay.debug`

**Версия:** 1.0-debug

**MinSDK:** 24 (Android 7.0)

**TargetSDK:** 33 (совместим с Android 16)

**Архитектуры:**
- arm64-v8a (для Pixel 8 Pro)
- armeabi-v7a (для старых ARM)
- x86 (для MEmu)
- x86_64 (для MEmu)

**Размер:** ~7-8 МБ

**Разрешения:**
- Internet
- Network State
- Notification Access (требует ручной настройки)
- Foreground Service
- Wake Lock
- Boot Completed

---

## 🆘 Всё ещё не работает?

### Покажите мне:

1. **Вывод команды установки:**
   ```bash
   adb install -r -d -t AdvancePay-compatible.apk
   ```

2. **Версию Android:**
   ```bash
   adb shell getprop ro.build.version.release
   adb shell getprop ro.build.version.sdk
   ```

3. **Скриншот ошибки** при установке через файл

### Альтернатива - Упрощённая версия

Могу создать совсем минималистичную версию:
- Без Foreground Service
- Без автозапуска
- Только базовый функционал

Это точно установится, но потребует держать приложение открытым.

---

**Удачи с установкой! 🚀**
