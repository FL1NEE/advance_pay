# 🔨 Инструкция по сборке APK

## ✅ Исправлены ошибки компиляции

Все ошибки исправлены:
- ✅ `LoginActivity.kt` - исправлен доступ к текстовым полям
- ✅ `MainActivity.kt` - исправлена регистрация BroadcastReceiver
- ✅ `build.gradle.kts` - обновлены версии плагинов

## 📋 Шаг 1: Установите Android Studio

1. Скачайте: https://developer.android.com/studio
2. Установите с настройками по умолчанию
3. При первом запуске дождитесь установки Android SDK

## 📂 Шаг 2: Откройте проект

1. Запустите Android Studio
2. **File → Open**
3. Выберите папку `AdvancePayApp`
4. Нажмите **OK**
5. Дождитесь синхронизации Gradle (5-10 минут при первом открытии)

**ВАЖНО:** Если появится предложение обновить Gradle Plugin - согласитесь (нажмите "Update")

## 🌐 Шаг 3: Настройте IP сервера

1. Найдите файл:
   ```
   app/src/main/java/com/advancepay/data/api/ApiService.kt
   ```

2. Откройте его (двойной клик)

3. Найдите строку 41:
   ```kotlin
   private const val BASE_URL = "http://10.0.2.2:8000/"
   ```

4. **Для эмулятора** оставьте как есть: `http://10.0.2.2:8000/`

5. **Для реального телефона** замените на IP вашего компьютера:

   **Windows - узнайте IP:**
   ```bash
   ipconfig
   # Найдите IPv4 Address в разделе WiFi
   ```

   **Linux/Mac - узнайте IP:**
   ```bash
   ifconfig | grep inet
   ```

   Замените на что-то вроде:
   ```kotlin
   private const val BASE_URL = "http://192.168.1.100:8000/"
   ```

6. Сохраните файл (**Ctrl+S** или **Cmd+S**)

## 🏗️ Шаг 4: Соберите APK

### Способ A: Через меню Android Studio

1. В меню: **Build → Build Bundle(s) / APK(s) → Build APK(s)**
2. Дождитесь сообщения "APK(s) generated successfully"
3. Нажмите **locate** - откроется папка с APK
4. Файл: `app-debug.apk`

### Способ B: Через терминал в Android Studio

1. Откройте Terminal внизу экрана
2. Выполните команду:

**Windows:**
```bash
gradlew.bat assembleDebug
```

**Linux/Mac:**
```bash
chmod +x gradlew
./gradlew assembleDebug
```

3. APK будет в: `app/build/outputs/apk/debug/app-debug.apk`

## 📱 Шаг 5: Установка на телефон

### Вариант 1: Через USB (рекомендуется)

**Подготовка телефона:**

1. Откройте **Настройки** на телефоне
2. **О телефоне** → 7 раз быстро нажмите на **Номер сборки**
   - Появится сообщение "Вы стали разработчиком!"
3. Вернитесь назад в Настройки
4. Найдите **Для разработчиков** (или **Developer Options**)
5. Включите **Отладку по USB** (USB Debugging)

**Установка:**

1. Подключите телефон к компьютеру через USB
2. На телефоне разрешите отладку (появится запрос)
3. В Android Studio: **Run → Run 'app'** (зеленая кнопка ▶️)
4. Или через терминал:
   ```bash
   adb install app/build/outputs/apk/debug/app-debug.apk
   ```

### Вариант 2: Через файл

1. Скопируйте `app-debug.apk` на телефон (любым способом)
2. Откройте файл на телефоне
3. Если появится запрос "Установка заблокирована":
   - Нажмите **Настройки**
   - Разрешите установку из этого источника
   - Вернитесь и нажмите **Установить**
4. Нажмите **Открыть** после установки

## ⚙️ Шаг 6: Первый запуск

### 1. Запустите backend на компьютере

```bash
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

Должно появиться:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 2. Запустите приложение на телефоне

- Откройте **AdvancePay**
- Введите логин и пароль
- Нажмите **Войти**

### 3. Предоставьте доступ к уведомлениям

**КРИТИЧЕСКИ ВАЖНО! Без этого приложение не будет работать!**

1. На главном экране нажмите **"Разрешить доступ к уведомлениям"**
2. Откроется экран настроек
3. Найдите **AdvancePay** в списке приложений
4. Включите переключатель
5. Подтвердите разрешение (если попросит)
6. Вернитесь в приложение

Предупреждение должно исчезнуть.

### 4. Отключите оптимизацию батареи

Чтобы приложение работало постоянно:

1. Откройте **Настройки** телефона
2. **Батарея** → **Оптимизация батареи** (или Battery Optimization)
3. В выпадающем меню выберите **Все приложения**
4. Найдите **AdvancePay**
5. Выберите **Не оптимизировать** (Don't optimize)
6. Нажмите **Готово**

### 5. Разрешите автозапуск (зависит от производителя)

**Xiaomi (MIUI):**
- Настройки → Приложения → Управление приложениями → AdvancePay → Автозапуск → ✅

**Huawei/Honor:**
- Настройки → Приложения → AdvancePay → Автозапуск → ✅

**Samsung:**
- Настройки → Приложения → AdvancePay → Батарея → Фоновая активность → Разрешить

**Oppo/Realme:**
- Настройки → Приложения → Запуск приложений → AdvancePay → Управлять вручную → ✅ Все пункты

**Vivo:**
- Настройки → Батарея → Фоновое потребление энергии → AdvancePay → Разрешить

## ✅ Шаг 7: Проверка работы

После всех настроек на главном экране должно показываться:

```
Статус: Работает
Батарея: XX%
Интернет: Есть
```

**Индикатор должен быть ЗЕЛЕНЫМ** ✅

### Тестирование:

1. Откройте любое банковское приложение (Сбербанк, Тинькофф и т.д.)
2. Просто зайдите в приложение - должно появиться какое-то уведомление
3. В AdvancePay нажмите **"История уведомлений"**
4. Должны появиться отправленные уведомления

## 🐛 Решение проблем

### "Ошибка компиляции" в Android Studio

**Проблема:** Проект не собирается

**Решение:**
1. **File → Invalidate Caches... → Invalidate and Restart**
2. После перезапуска: **Build → Clean Project**
3. Потом: **Build → Rebuild Project**

### "Failed to resolve" или "Could not download"

**Проблема:** Gradle не может скачать зависимости

**Решение:**
1. Проверьте интернет соединение
2. Откройте `build.gradle.kts` (модуль app)
3. **Build → Sync Project with Gradle Files**

### "SDK location not found"

**Проблема:** Android Studio не знает где Android SDK

**Решение:**
1. **File → Project Structure**
2. **SDK Location** → укажите путь к SDK
   - Windows: обычно `C:\Users\ИМЯ\AppData\Local\Android\Sdk`
   - Mac: обычно `/Users/ИМЯ/Library/Android/sdk`
3. Нажмите **OK**

### "Ошибка подключения" при входе в приложение

**Проблема:** Приложение не может подключиться к серверу

**Проверьте:**

1. ✅ Backend запущен:
   ```bash
   cd backend
   uvicorn app.main:app --host 0.0.0.0 --port 8000
   ```

2. ✅ Телефон и компьютер в одной WiFi сети

3. ✅ IP адрес правильный в `ApiService.kt`:
   - Для эмулятора: `10.0.2.2`
   - Для реального телефона: IP компьютера (проверьте через `ipconfig`)

4. ✅ Firewall не блокирует порт 8000:
   - Windows: разрешите Python в брандмауэре
   - Linux: `sudo ufw allow 8000`

5. ✅ Попробуйте открыть в браузере телефона:
   ```
   http://ВАШ_IP:8000/health
   ```
   Должно показать: `{"status":"healthy"}`

### Приложение не ловит уведомления

**Проблема:** Уведомления не отправляются на сервер

**Проверьте:**

1. ✅ Дан доступ к уведомлениям (см. Шаг 6.3)
2. ✅ Батарея больше 30%
3. ✅ Есть интернет
4. ✅ Статус показывает "Работает"

**Перезапуск:**
1. Закройте приложение полностью (из списка недавних)
2. Перезапустите телефон
3. Откройте приложение снова

### Приложение закрывается само

**Проблема:** Android убивает приложение для экономии батареи

**Решение:**
1. Отключите оптимизацию батареи (Шаг 6.4)
2. Разрешите автозапуск (Шаг 6.5)
3. Разрешите работу в фоне

**Дополнительно для Xiaomi:**
- Настройки → Приложения → AdvancePay → Контроль активности → Нет ограничений

## 📊 Просмотр логов

Для отладки подключите телефон и используйте:

```bash
adb logcat | grep -E "AdvancePay|BankNotification"
```

Или в Android Studio:
- **Logcat** (вкладка внизу)
- Фильтр: `package:com.advancepay`

## 🎯 Поддерживаемые банки

Приложение отслеживает уведомления от:

- 🏦 Сбербанк
- 💳 Тинькофф
- 🏛️ ВТБ
- 🔷 Альфа-Банк
- 💰 Райффайзен
- 📱 МТС Банк
- 🏦 Россельхозбанк
- 💼 ЮниКредит
- 🏦 Росбанк
- 📲 Совесть
- 🏦 Открытие
- 💳 ПромСвязьБанк

И другие банковские приложения.

## 📞 Нужна помощь?

1. Проверьте логи (см. выше)
2. Убедитесь что выполнены ВСЕ шаги из инструкции
3. Проверьте что backend работает: откройте `http://localhost:8000/docs`

---

**Успешной сборки! 🚀**
