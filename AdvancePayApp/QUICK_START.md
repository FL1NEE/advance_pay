# 🚀 Быстрая инструкция по сборке APK

## Шаг 1: Установите Android Studio

1. Скачайте Android Studio: https://developer.android.com/studio
2. Установите с настройками по умолчанию
3. При первом запуске дождитесь скачивания всех компонентов

## Шаг 2: Откройте проект

1. Запустите Android Studio
2. Нажмите **File → Open**
3. Выберите папку `AdvancePayApp`
4. Нажмите **OK**
5. Дождитесь индексации проекта (может занять 5-10 минут)

## Шаг 3: Настройте адрес сервера

1. В левой панели найдите:
   ```
   app → src → main → java → com.advancepay → data → api → ApiService.kt
   ```

2. Откройте файл двойным кликом

3. Найдите строку (примерно 59-я строка):
   ```kotlin
   private const val BASE_URL = "http://10.0.2.2:8000/"
   ```

4. Замените на IP вашего компьютера:
   - Узнайте свой IP: `ipconfig` (Windows) или `ifconfig` (Linux/Mac)
   - Замените на что-то вроде: `"http://192.168.1.100:8000/"`

5. Сохраните файл (Ctrl+S)

## Шаг 4: Соберите APK

### Вариант A: Через меню (проще)

1. Нажмите **Build** в верхнем меню
2. Выберите **Build Bundle(s) / APK(s)**
3. Нажмите **Build APK(s)**
4. Дождитесь сообщения "APK(s) generated successfully"
5. Нажмите **locate** чтобы открыть папку с APK

### Вариант B: Через терминал

1. В Android Studio откройте терминал (внизу экрана)
2. Выполните команду:

   **Windows:**
   ```bash
   gradlew.bat assembleDebug
   ```

   **Linux/Mac:**
   ```bash
   ./gradlew assembleDebug
   ```

3. APK будет в: `app/build/outputs/apk/debug/app-debug.apk`

## Шаг 5: Установите на телефон

### Способ 1: Через USB (рекомендуется)

1. **Включите отладку на телефоне:**
   - Откройте **Настройки**
   - **О телефоне** → 7 раз нажмите на **Номер сборки**
   - Вернитесь назад → **Для разработчиков** → включите **Отладку по USB**

2. **Подключите телефон к компьютеру**

3. **Установите:**
   - В Android Studio: **Run → Run 'app'**
   - Или через терминал: `adb install app/build/outputs/apk/debug/app-debug.apk`

### Способ 2: Через файл

1. Скопируйте файл `app-debug.apk` на телефон
2. Откройте файл на телефоне
3. Разрешите установку из неизвестных источников
4. Нажмите **Установить**

## Шаг 6: Настройте приложение

### 1. Запустите приложение

- Введите логин и пароль от backend
- Нажмите "Войти"

### 2. Дайте доступ к уведомлениям (ОБЯЗАТЕЛЬНО!)

- Нажмите кнопку **"Разрешить доступ к уведомлениям"**
- Найдите **AdvancePay** в списке
- Включите переключатель
- Вернитесь в приложение

### 3. Отключите оптимизацию батареи

**Важно для постоянной работы!**

1. Откройте **Настройки** телефона
2. **Батарея** → **Оптимизация батареи**
3. Найдите **AdvancePay**
4. Выберите **Не оптимизировать**

### 4. Разрешите автозапуск (зависит от телефона)

**Xiaomi:**
- Настройки → Приложения → AdvancePay → Автозапуск → Включить

**Huawei:**
- Настройки → Приложения → AdvancePay → Автозапуск

**Samsung:**
- Настройки → Приложения → AdvancePay → Батарея → Фоновая активность

## ✅ Готово!

Теперь приложение должно показывать статус **"Работает"** и автоматически отправлять банковские уведомления на сервер.

## 🔍 Проверка работы

1. Откройте любое банковское приложение
2. Создайте тестовое уведомление (можно просто зайти в приложение банка)
3. В AdvancePay нажмите **"История уведомлений"**
4. Должны появиться отправленные уведомления

## ❓ Что-то не работает?

### Приложение не собирается

- Убедитесь что установлены все компоненты Android Studio
- Попробуйте: **File → Invalidate Caches → Invalidate and Restart**
- Проверьте интернет соединение (Gradle скачивает зависимости)

### "Ошибка подключения" при входе

- Убедитесь что backend запущен: `uvicorn app.main:app --host 0.0.0.0 --port 8000`
- Проверьте что телефон и компьютер в одной WiFi сети
- Проверьте правильность IP адреса в `ApiService.kt`

### Приложение не ловит уведомления

- Проверьте что дали доступ к уведомлениям
- Убедитесь что батарея > 30%
- Проверьте что есть интернет
- Перезапустите приложение

## 📱 Поддерживаемые версии Android

- Минимальная: Android 7.0 (API 24)
- Рекомендуемая: Android 10 и выше

## 🆘 Нужна помощь?

Проверьте логи:
```bash
adb logcat | grep -E "BankNotification|MonitoringService"
```

Или в Android Studio: **Logcat** (внизу) → фильтр "AdvancePay"
