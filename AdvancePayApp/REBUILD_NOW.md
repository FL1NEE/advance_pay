# ⚡ СРОЧНО: Пересоберите APK

Старый APK не работает на Android 12+ и Android 16.
Проект обновлён - нужно пересобрать!

## 🎯 Самый простой способ (5 минут):

### 1. Откройте Android Studio

Если не установлен - скачайте: https://developer.android.com/studio

### 2. Откройте проект

```
File → Open
```

Выберите папку: `AdvancePayApp`

### 3. Дождитесь синхронизации

Внизу появится:
```
Gradle sync in progress...
```

Дождитесь окончания (5-10 минут при первом открытии)

**Если появится "Update Gradle Plugin" - нажмите UPDATE!**

### 4. Настройте IP сервера (ВАЖНО!)

Если у вас **реальный телефон** (не эмулятор):

1. Узнайте IP компьютера:
   ```bash
   # Windows:
   ipconfig

   # Найдите IPv4 Address, например: 192.168.1.100
   ```

2. Откройте файл в Android Studio:
   ```
   app → src → main → java → com.advancepay → data → api → ApiService.kt
   ```

3. Измените строку 41:
   ```kotlin
   // Было (для эмулятора):
   private const val BASE_URL = "http://10.0.2.2:8000/"

   // Станет (для реального телефона):
   private const val BASE_URL = "http://192.168.1.100:8000/"  // ← ВАШ IP!
   ```

4. Сохраните (Ctrl+S)

**Для эмулятора** - ничего не меняйте!

### 5. Соберите APK

В меню:
```
Build → Clean Project
```

Дождитесь окончания, затем:
```
Build → Build Bundle(s) / APK(s) → Build APK(s)
```

### 6. Найдите APK

Появится уведомление:
```
APK(s) generated successfully
```

Нажмите **locate** - откроется папка с APK.

Файл называется: `app-debug.apk`

## 📱 Установка

### На Pixel 8 Pro (Android 16):

```bash
adb install -r app-debug.apk
```

Если старая версия мешает:
```bash
adb uninstall com.advancepay
adb install app-debug.apk
```

### На эмулятор Android 12:

1. Запустите эмулятор в Android Studio
2. Перетащите `app-debug.apk` в окно эмулятора

Или:
```bash
adb install app-debug.apk
```

## ✅ Проверка

После установки:

1. Запустите AdvancePay
2. Войдите (логин/пароль)
3. Дайте доступ к уведомлениям
4. Статус должен показать "Работает" ✅

## 🐛 Если не собирается

### Ошибка: "SDK location not found"

1. **File → Project Structure**
2. **SDK Location** → укажите путь к Android SDK
3. Обычно: `C:\Users\ИМЯ\AppData\Local\Android\Sdk`
4. OK

### Ошибка: "Failed to resolve dependencies"

1. **File → Invalidate Caches → Invalidate and Restart**
2. После перезапуска: **Build → Clean Project**
3. Потом: **Build → Rebuild Project**

### Ошибка компиляции в коде

Проект уже исправлен, но если что-то пошло не так:

1. **Build → Clean Project**
2. **File → Sync Project with Gradle Files**
3. **Build → Rebuild Project**

## 📞 Всё ещё не работает?

Напишите мне какую ошибку видите:

- При сборке APK - скриншот ошибки из Android Studio
- При установке - выполните:
  ```bash
  adb install -r app-debug.apk
  ```
  И покажите вывод

## 🎁 Бонус: Автоматическая сборка

Если Android Studio не хотите:

**Windows:**
```bash
cd AdvancePayApp
build_apk.bat
```

**Linux/Mac:**
```bash
cd AdvancePayApp
chmod +x build_apk.sh
./build_apk.sh
```

APK появится в корне: `AdvancePay.apk`

---

## ⚡ БЫСТРАЯ ПАМЯТКА:

```
1. Открыть в Android Studio
2. Дождаться Gradle Sync
3. (Опционально) Изменить IP в ApiService.kt
4. Build → Clean Project
5. Build → Build APK
6. adb install -r app-debug.apk
7. Готово!
```

**Удачи! 🚀**
