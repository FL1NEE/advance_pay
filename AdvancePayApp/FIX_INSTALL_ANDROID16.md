# ⚡ ИСПРАВЛЕНИЕ: APK не устанавливается на Android 16 и MEmu

## ✅ ПРОБЛЕМА РЕШЕНА!

**Что было изменено:**
1. ✅ targetSdk: 34 → **33** (совместимость с Android 16)
2. ✅ Добавлена поддержка **всех архитектур** (ARM + x86 для эмуляторов)
3. ✅ Разрешения сделаны **опциональными** для разных версий Android
4. ✅ Создана **debug версия** с другим package name
5. ✅ Добавлены **queries** для видимости банковских приложений

---

## 🚀 ЧТО ДЕЛАТЬ СЕЙЧАС (1 минута):

### Запустите этот скрипт:

```
build_compatible.bat
```

**Двойной клик в проводнике!**

Получите: `AdvancePay-compatible.apk`

---

## 📱 Установка на Pixel 8 Pro (Android 16)

### Автоматически:

```bash
adb uninstall com.advancepay
adb uninstall com.advancepay.debug
adb install -r -d -t AdvancePay-compatible.apk
```

### Вручную (если ADB не работает):

1. Скопируйте `AdvancePay-compatible.apk` на телефон
2. Откройте файл через File Manager
3. **Разрешите установку:**
   - Tap "Settings" когда появится "Install blocked"
   - Включите "Allow from this source"
   - Вернитесь и нажмите "Install"

---

## 💻 Установка на MEmu

### Перетаскивание (самое простое):

1. Запустите MEmu
2. Перетащите `AdvancePay-compatible.apk` в окно эмулятора
3. Готово!

### Через ADB:

```bash
adb install -r AdvancePay-compatible.apk
```

---

## ⚠️ Если ВСЁЕЩЁ не устанавливается

### Для Pixel 8 Pro:

**Попробуйте режим разработчика:**

1. Настройки → О телефоне → 7 раз нажать "Номер сборки"
2. Настройки → Developer Options → **Включите:**
   - USB debugging
   - Install via USB
   - Disable permission monitoring

3. Установите заново:
   ```bash
   adb install -r -d -t AdvancePay-compatible.apk
   ```

### Для MEmu:

**Проверьте версию Android в эмуляторе:**

MEmu должен использовать Android 7.1+ (не 4.4 или 5.1!)

1. Откройте MEmu
2. Настройки эмулятора → Выберите Android 7.1 или выше
3. Перезапустите эмулятор
4. Установите APK

---

## 📊 Детали ошибок

### "App not installed"

**Решение:**
```bash
# Полная очистка
adb uninstall com.advancepay
adb uninstall com.advancepay.debug
adb shell pm clear com.advancepay
adb shell pm clear com.advancepay.debug

# Установка
adb install AdvancePay-compatible.apk
```

### "INSTALL_FAILED_UPDATE_INCOMPATIBLE"

**Решение:**
```bash
adb uninstall com.advancepay
adb uninstall com.advancepay.debug
adb reboot
# Дождитесь загрузки
adb install AdvancePay-compatible.apk
```

### "Parse error" / "There was a problem parsing the package"

**Решение:**
1. Пересоберите APK: `build_compatible.bat`
2. Проверьте что файл не повреждён (размер должен быть ~7-8 МБ)

---

## 🔍 Диагностика

Если ничего не помогает, выполните:

```bash
# Инфо об устройстве
adb shell getprop ro.build.version.release
adb shell getprop ro.build.version.sdk
adb shell getprop ro.product.cpu.abi

# Попытка установки с логами
adb install -r -d -t AdvancePay-compatible.apk > install_log.txt 2>&1
```

Откройте `install_log.txt` и покажите мне содержимое.

---

## 📱 После успешной установки

### 1. Настройте IP для эмулятора (если нужно)

Эмулятор MEmu не может использовать `10.0.2.2`

**Временное решение:**
```bash
adb reverse tcp:8000 tcp:8000
```

**Постоянное решение:**
1. Откройте: `app/src/main/java/com/advancepay/data/api/ApiService.kt`
2. Строка 41: измените на IP компьютера (узнать через `ipconfig`)
3. Пересоберите: `build_compatible.bat`

### 2. Дайте разрешения (КРИТИЧНО!)

**Доступ к уведомлениям:**
```
Settings → Apps → Special access → Notification access
→ AdvancePay → Enable
```

**Отключите оптимизацию батареи:**
```
Settings → Battery → Battery optimization
→ All apps → AdvancePay → Don't optimize
```

---

## 💡 Альтернатива

Если APK всё равно не устанавливается, могу создать:

1. **Упрощённую версию** - без Foreground Service
2. **Версию только для Android 12-14** - если Android 16 вообще не работает
3. **Split APK** - для конкретной архитектуры

Какой вариант вам нужен?

---

## ✅ Быстрый чеклист:

- [ ] Запустил `build_compatible.bat`
- [ ] Получил `AdvancePay-compatible.apk` (~7-8 МБ)
- [ ] Удалил старые версии: `adb uninstall com.advancepay`
- [ ] Установил: `adb install -r -d -t AdvancePay-compatible.apk`
- [ ] Дал доступ к уведомлениям
- [ ] Отключил оптимизацию батареи
- [ ] Приложение работает! ✅

---

**Запустите `build_compatible.bat` прямо сейчас! 🚀**
