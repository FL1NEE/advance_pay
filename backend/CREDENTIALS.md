# 🔑 Учетные данные для входа

## Создание тестового пользователя

### Автоматически:

```bash
cd backend
python create_test_user.py
```

Будет создан пользователь:
- **Логин:** `trader1`
- **Пароль:** `test123`
- **Роль:** trader

### Вручную через API:

Убедитесь что backend запущен:
```bash
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

Откройте браузер:
```
http://localhost:8000/docs
```

Найдите **POST /api/v1/auth/register** и выполните:
```json
{
  "username": "trader1",
  "password": "test123",
  "email": "trader1@example.com",
  "role": "trader"
}
```

---

## Готовые тестовые пользователи

### Trader (трейдер):
- **Логин:** `trader1`
- **Пароль:** `test123`
- **Роль:** trader

### Можно создать других:

**TeamLead (тимлид):**
```json
{
  "username": "teamlead1",
  "password": "test123",
  "role": "teamlead"
}
```

**Support (поддержка):**
```json
{
  "username": "support1",
  "password": "test123",
  "role": "support"
}
```

---

## Использование в приложении

1. Запустите backend
2. Создайте пользователя (скрипт выше)
3. Откройте приложение на телефоне
4. Введите логин и пароль
5. Готово!

---

## Если забыли пароль

Пароли хранятся в зашифрованном виде. Чтобы сбросить:

1. Удалите базу данных:
   ```bash
   cd backend
   rm advancepay.db
   ```

2. Перезапустите backend (база пересоздастся)

3. Создайте пользователя заново

---

## Проверка существующих пользователей

Откройте базу данных:
```bash
cd backend
sqlite3 advancepay.db
```

Выполните:
```sql
SELECT username, role, is_active FROM users;
```

Выход: `.quit`
